#!/usr/bin/env ruby

# Example of usage: rake -g name_of_rake_task
task :default => :default_rake_task

task :default_rake_task do
  puts 'Example of usage: rake -g name_of_rake_task'

  require 'etc'
  puts Etc.getlogin
end
